<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cykj.housewifery.mapper.OrderMapper">

    <select id="getOrderCount" resultType="int">
       select count(*) from tb_order where company=(select id from tb_company where name=#{arg0})
    </select>

    <select id="selOrder" resultType="Order">
        select id,(select )userId from tb_order
    </select>
</mapper>